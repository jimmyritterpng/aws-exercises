using Amazon.CDK;
using Constructs;

namespace Exercises
{
    public class ExercisesStack : Stack
    {
        internal ExercisesStack(Construct scope, string id, IStackProps props = null) : base(scope, id, props)
        {
            // The code that defines your stack goes here
        }
    }
}
